<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ActionButton">
        <item name="android:clickable">true</item>
        <item name="android:background">@color/royal_blue</item>
        <item name="android:layout_marginLeft">16dp</item>
        <item name="android:layout_marginRight">16dp</item>
        <item name="android:layout_marginBottom">50dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/_18sdp</item>
    </style>

    <style name="CairoRegular">
        <!--item name="fontPath">@string/cairo_regular</item-->
        <item name="android:fontFamily">@font/cairo_regular</item>

        <item name="android:textStyle">normal</item>
    </style>

    <style name="ActionButton.NextStepMode">
        <item name="android:background">@drawable/action_btn_next_step_mode_background</item>
        <item name="android:textAppearance">@style/CairoRegular</item>
        <item name="android:textSize">@dimen/_18sdp</item>
    </style>
</resources>
